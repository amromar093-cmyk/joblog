DATABASE_URL="postgresql://user:password@localhost:5432/joblog"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000

# Optional — only needed for the AI match/cover-letter routes and `npm run
# eval`. The app boots and every other route works fine without these; the
# AI routes 503 with a clear message instead.
ANTHROPIC_API_KEY=""
VOYAGE_API_KEY=""
