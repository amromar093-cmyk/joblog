services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: joblog
      POSTGRES_PASSWORD: joblog
      POSTGRES_DB: joblog
    ports:
      - "5432:5432"
    volumes:
      - joblog-db:/var/lib/postgresql/data

volumes:
  joblog-db:
